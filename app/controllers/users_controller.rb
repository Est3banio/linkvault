# frozen_string_literal: true

class UsersController < ApplicationController
  # This controller is no longer needed as Devise handles user registration
  # Keeping the file as a reference in case we need to customize Devise controllers later
end
